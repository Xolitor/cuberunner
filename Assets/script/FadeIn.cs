using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class FadeIn : MonoBehaviour
{
    /*public CanvasGroup uiElements;

    private void Start()
    {
        StartCoroutine(loadMenu(uiElements));
    }


    public IEnumerator loadMenu(CanvasGroup cg)
    {
        yield return new WaitForSeconds(1f);
        for(uiElements.alpha=0; uiElements.alpha <= 1; uiElements.alpha += Time.deltaTime)
        {
            yield return null;
        }
    }*/
}
